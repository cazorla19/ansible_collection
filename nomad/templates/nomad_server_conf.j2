data_dir = "{{ data_dir }}"
datacenter = "aws_oregon"

server {
    enabled = true
    bootstrap_expect = 1
}

consul {
    address = "127.0.0.1:8500"
}

atlas {
  join = true
  infrastructure = "{{ atlas_user }}/mars"
  token = "{{ atlas_token }}"
}